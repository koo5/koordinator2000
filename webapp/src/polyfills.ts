/**
 * Browser polyfills for compatibility
 * This is intentionally minimal as modern browsers support most features natively
 */
if (typeof window !== 'undefined') {
  // Add any required polyfills here
}

export default {};